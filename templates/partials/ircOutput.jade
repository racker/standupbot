|---------------------------------------#{nl}
- for line in completed
  - if (line.length > 1 || completed.length == 1)
    |[#{irc_nick}: #{area} completed ] #{line}#{nl}
- for line in inprogress
  - if (line.length > 1 || inprogress.length == 1)
    |[#{irc_nick}: #{area} inprogress ] #{line}#{nl}
- for line in impediments
  - if (line.length > 1 || impediments.length == 1)
    |[#{irc_nick}: #{area} impediments ] #{line}#{nl}
|---------------------------------------#{nl}
